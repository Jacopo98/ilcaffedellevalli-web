"use client";import Link from "next/link";import {useRouter} from "next/navigation";import {useState,useTransition} from "react";import {ArrowLeft,Bell,CalendarClock,Check,Plus,StickyNote,X} from "lucide-react";import {createAdminNote,markNotification,updateEmailPreference,type NotificationResult} from "./actions";
type Notice={id:number;kind:"payment_due"|"note"|"system";title:string;message:string|null;due_date:string|null;priority:"normal"|"high";is_read:boolean;created_at:string};
export function NotificationCenter({notifications,setupReady,email,emailEnabled,emailPreferenceReady}:{notifications:Notice[];setupReady:boolean;email:string;emailEnabled:boolean;emailPreferenceReady:boolean}){const router=useRouter();const [editor,setEditor]=useState(false);const [pending,startTransition]=useTransition();const [error,setError]=useState("");function run(action:(d:FormData)=>Promise<NotificationResult>,data:FormData,done=()=>{}){startTransition(async()=>{const r=await action(data);if(!r.ok){setError(r.error||"Errore");return}done();router.refresh()})}return <main className="admin-container notifications-page"><div className="staff-topbar"><Link className="admin-inline-link" href="/admin"><ArrowLeft size={15}/> Dashboard</Link><button className="admin-action admin-action-primary admin-action-create" onClick={()=>setEditor(true)}><Plus size={15}/> Nuova nota</button></div><header className="staff-heading"><div><p className="admin-kicker">Centro amministrativo</p><h1>Notifiche e note</h1><p>Scadenze imminenti, promemoria di pagamento e annotazioni interne.</p></div><div className="notification-count"><Bell/><strong>{notifications.filter(n=>!n.is_read).length}</strong><span>da leggere</span></div></header><section className="notification-email-preference"><div><strong>Promemoria via email</strong><p>Ricevi su {email} il riepilogo delle scadenze previste entro sette giorni.</p></div><label className="notification-email-toggle"><span>{emailEnabled?"Attive":"Disattivate"}</span><input type="checkbox" checked={emailEnabled} disabled={pending||!emailPreferenceReady} onChange={event=>{const data=new FormData();data.set("enabled",String(event.target.checked));run(updateEmailPreference,data)}}/><i/></label></section>{!emailPreferenceReady&&<div className="staff-alert">Esegui notification-email-preferences-migration.sql per attivare le preferenze email.</div>}{!setupReady&&<div className="staff-alert">Esegui workforce-notifications-migration.sql su Supabase.</div>}{error&&<div className="staff-alert">{error}</div>}<section className="notification-list">{notifications.map(n=><article className={`${n.is_read?"read":""} ${n.priority==="high"?"high":""}`} key={n.id}><span className="notification-icon">{n.kind==="note"?<StickyNote/>:<CalendarClock/>}</span><div><small>{n.kind==="note"?"Nota interna":"Scadenza pagamento"}{n.due_date?` · ${new Date(`${n.due_date}T12:00:00Z`).toLocaleDateString("it-IT")}`:""}</small><h2>{n.title}</h2>{n.message&&<p>{n.message}</p>}</div>{!n.is_read&&<button aria-label="Segna come letta" onClick={()=>{const d=new FormData();d.set("id",String(n.id));run(markNotification,d)}}><Check/></button>}</article>)}{!notifications.length&&<div className="notification-empty"><Bell/><h2>Nessuna notifica</h2><p>Le prossime scadenze appariranno qui.</p></div>}</section>{editor&&<div className="admin-modal-backdrop"><section className="admin-modal"><button className="modal-close" onClick={()=>setEditor(false)}><X/></button><p className="admin-kicker">Promemoria</p><h2>Nuova nota</h2><form className="admin-edit-grid" onSubmit={e=>{e.preventDefault();run(createAdminNote,new FormData(e.currentTarget),()=>setEditor(false))}}><label className="admin-field admin-field-wide"><span>Titolo</span><input name="title" required/></label><label className="admin-field"><span>Scadenza opzionale</span><input name="due_date" type="date"/></label><label className="admin-field"><span>Priorità</span><select name="priority"><option value="normal">Normale</option><option value="high">Alta</option></select></label><label className="admin-field admin-field-wide"><span>Nota</span><textarea name="message" rows={4}/></label><div className="modal-actions admin-field-wide"><button type="button" className="admin-action admin-action-secondary" onClick={()=>setEditor(false)}>Annulla</button><button className="admin-action admin-action-primary" disabled={pending}>Salva nota</button></div></form></section></div>}</main>}
