"use server";
import {revalidatePath} from "next/cache";import {z} from "zod";import {requireAdmin} from "@/lib/auth";
export type NotificationResult={ok:boolean;error?:string};
export async function markNotification(formData:FormData):Promise<NotificationResult>{const id=z.coerce.number().int().positive().safeParse(formData.get("id"));if(!id.success)return{ok:false,error:"Notifica non valida"};const {supabase}=await requireAdmin();const {error}=await supabase.from("admin_notifications").update({is_read:true,read_at:new Date().toISOString()}).eq("id",id.data);if(error)return{ok:false,error:"Impossibile aggiornare la notifica"};revalidatePath("/admin/notifiche");return{ok:true}}
export async function createAdminNote(formData:FormData):Promise<NotificationResult>{const parsed=z.object({title:z.string().trim().min(1).max(160),message:z.string().trim().max(2000),due:z.union([z.literal(""),z.iso.date()]).transform(v=>v||null),priority:z.enum(["normal","high"])}).safeParse({title:formData.get("title"),message:formData.get("message")??"",due:formData.get("due_date")??"",priority:formData.get("priority")??"normal"});if(!parsed.success)return{ok:false,error:"Controlla la nota"};const {supabase}=await requireAdmin();const {error}=await supabase.from("admin_notifications").insert({kind:"note",title:parsed.data.title,message:parsed.data.message||null,due_date:parsed.data.due,priority:parsed.data.priority});if(error)return{ok:false,error:"Impossibile creare la nota"};revalidatePath("/admin/notifiche");return{ok:true}}
export async function updateEmailPreference(formData:FormData):Promise<NotificationResult>{const enabled=formData.get("enabled")==="true";const {supabase,profile}=await requireAdmin();const {error}=await supabase.from("profiles").update({receive_email_notifications:enabled}).eq("id",profile.id);if(error)return{ok:false,error:"Impossibile aggiornare la preferenza email. Esegui la migrazione dedicata."};revalidatePath("/admin/notifiche");return{ok:true}}
